#include<stdio.h>

#define max 100

int main()
{
    
    int a[max],b[max];
    int n,m,counter;
    while(Scanf("%d",&n) != EOF){
        for(int i = 0;i < n;i++){
            scanf("%d",&a[i]);
        }
        scanf("%d",&m);
        for(int i = 0;i < m;i++){
            scanf("%d",&b[i]);
        }
        for(int i = 0;i < m;i++){
            for(int j = 0;j < n;j++){
                if(a[j] == b[i]){
                    
                }
            }
            printf("YES\n");
        }
    }
    return 0;
}